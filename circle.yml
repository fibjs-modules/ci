machine:
  services:
    - docker

test:
  override:
    - docker run -d -p 9200:9200 fibjs/fibjs:nightly; sleep 10
    - docker run fibjs/fibjs:nightly ls -la
    - docker run fibjs/fibjs:nightly fibjs app.js
    - ls -la
    - fibjs app.js
