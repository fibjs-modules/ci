sudo: required

language: python

services:
  - docker

before_install:
- docker run -d --name app fibjs/fibjs:nightly
- docker ps -a

script:
- docker exec app python -m unittest discover

after_script:
- docker rm -f app