sudo: required

services:
  - docker

script:
- docker pull fibjs/fibjs:v0.2.1
- sudo dir=`pwd`; docker run -v ${dir}:/home fibjs/fibjs:v0.2.1 ls -la /home ;cd /home; ls -la ; npm install; npm run test;
